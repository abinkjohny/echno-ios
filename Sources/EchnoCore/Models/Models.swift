// Models — one folder per domain module.
//
// Hand-written Swift over the generated `EchnoAPI` wire layer: domain structs
// with non-optional invariants, the mapping that unwraps them once, the service
// actor, and the observable store.
//
// The contract is the backend's OpenAPI document, not any client library. See
// `local-docs/ios-action-plan.md` §1.
